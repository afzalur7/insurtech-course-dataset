# Python packages for the Insurtech & Digital Risk Solutions course scripts
# Install in a virtual environment to avoid system-package conflicts:
#
#   python3 -m venv venv
#   venv/bin/pip install -r requirements.txt
#   venv/bin/python data_analysis.py
#   venv/bin/python session-06/visualization_analysis.py
#
# Version notes:
# - pandas >= 2.2 so the monthly alias 'ME' is supported
# - numpy and matplotlib are installed together by pip to keep them compatible
#   (mixing a system matplotlib/scipy with a pip numpy breaks imports)

numpy>=1.26
pandas>=2.2
matplotlib>=3.8
seaborn>=0.13
scipy>=1.11
scikit-learn>=1.4
